// MapTiler key. Swap this for an ORIGIN-RESTRICTED key before go-live.
// Restrict the key at maptiler.com to: nishantn15.github.io  (and localhost for testing)
window.DIVE_CONFIG = {
  MAPTILER_KEY: "a23zWelrdy0JEQutUNBd",
  // basemap style: "satellite" | "hybrid" | "streets" | "ocean"
  STYLE: "hybrid",
};
